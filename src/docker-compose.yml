version: '3.4'

services:
  catalogdb:
    image: postgres

  basketdb:
    image: postgres

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalog.api
    build:
      context: .
      dockerfile: ./Dockerfile

volumes:
  postgres_catalog:
  postgres_basket: