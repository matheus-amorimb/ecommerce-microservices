version: '3.4'

services:
  catalogdb:
    image: postgres

  basketdb:
    image: postgres
    
  cache:
    image: redis

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalog.api
    build:
      context: .
      dockerfile: ./Dockerfile

volumes:
  postgres_catalog:
  postgres_basket: